version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build-prod
  post_build:
    commands:
      - echo Post build phase!
artifacts:
  files:
    - ./dist/**/*
  name: about-myself-$(date +%Y-%m-%d)
cache:
  paths:
    - ./node_modules